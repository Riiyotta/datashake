{
  "$comment": "Section contract. content is a JSON Schema fragment; every $ref (#/definitions/...) resolves inside schema/pagespec.schema.json, where this exact fragment is embedded as definitions.content_shell-navbar (verify_all.py checks they are identical). maxWords = ceil(1.25 x real observed max), min observed+1; x-observedMaxWords records the real observed count.",
  "id": "section.shell.navbar",
  "nodeSection": "shell.navbar",
  "category": "NAV",
  "purpose": "Fixed global navigation: four hover dropdowns, Login + Book a call, collapsing to a hamburger overlay at <=991.",
  "sourceComponent": "src/components/Navbar.jsx",
  "variants": {
    "default": {
      "pageLevel": true,
      "description": "The only variant observed."
    }
  },
  "content": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "brand",
      "menus",
      "loginCta",
      "bookCta"
    ],
    "properties": {
      "brand": {
        "allOf": [
          {
            "$ref": "#/definitions/media"
          },
          {
            "properties": {
              "assetRole": {
                "enum": [
                  "brand-logo"
                ]
              }
            }
          }
        ]
      },
      "menus": {
        "type": "array",
        "minItems": 1,
        "maxItems": 4,
        "items": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "label",
            "columns",
            "items"
          ],
          "properties": {
            "label": {
              "type": "string",
              "minLength": 1,
              "maxWords": 2,
              "x-observedMaxWords": 1
            },
            "columns": {
              "enum": [
                1,
                2
              ]
            },
            "items": {
              "type": "array",
              "minItems": 1,
              "maxItems": 8,
              "items": {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "title",
                  "description",
                  "href",
                  "icon"
                ],
                "properties": {
                  "title": {
                    "type": "string",
                    "minLength": 1,
                    "maxWords": 7,
                    "x-observedMaxWords": 5
                  },
                  "description": {
                    "type": "string",
                    "minLength": 1,
                    "maxWords": 8,
                    "x-observedMaxWords": 6
                  },
                  "href": {
                    "$ref": "#/definitions/href"
                  },
                  "icon": {
                    "allOf": [
                      {
                        "$ref": "#/definitions/media"
                      },
                      {
                        "properties": {
                          "assetRole": {
                            "enum": [
                              "ui-icon"
                            ]
                          }
                        }
                      }
                    ]
                  }
                }
              }
            }
          }
        }
      },
      "loginCta": {
        "$ref": "#/definitions/cta"
      },
      "bookCta": {
        "$ref": "#/definitions/cta"
      }
    }
  },
  "constraints": {
    "onePerPage": true,
    "mustBeFirst": true
  },
  "motion": {
    "allowedPatterns": [
      "dropdown-reveal",
      "hamburger-morph",
      "mobile-menu-slide",
      "nav-logo-spin"
    ],
    "reducedMotionFallback": "Dropdowns and the mobile overlay show/hide instantly with no translate/opacity tween; the hamburger swaps to its open state without the rotate tween; the brand mark does not spin on hover.",
    "fallbackSource": "prescriptive-design-repo-policy",
    "fallbackNote": "The original site has no prefers-reduced-motion handling (cite:spec-no-reduced-motion); this fallback is design-repo policy, not measured source behaviour.",
    "measuredFrom": [
      "cite:nav-brand-rotate-src",
      "cite:nav-dropdown-timing",
      "cite:nav-hamburger",
      "cite:nav-mobile-400",
      "cite:nav-slide",
      "live:nav-logo-spin"
    ]
  },
  "responsive": {
    "desktop": {
      "layout": "unchanged",
      "changes": [
        "Inline menu with 4 hover dropdowns + Login/Book a call."
      ],
      "measuredFrom": [
        "cite:nav-menus"
      ]
    },
    "tablet": {
      "layout": "overlay-menu",
      "changes": [
        "Desktop menu hidden; hamburger + compact 'Book a call' shown",
        "Dropdowns become tap-to-open stacked rows inside a 90vh overlay"
      ],
      "measuredFrom": [
        "cite:nav-tablet-collapse",
        "cite:nav-slide"
      ]
    },
    "mobileLandscape": {
      "layout": "overlay-menu",
      "changes": [
        "Buttons min-height 50px"
      ],
      "measuredFrom": [
        "cite:ui-outline-variants"
      ]
    },
    "mobilePortrait": {
      "layout": "overlay-menu",
      "changes": [
        "Overlay min-height 80vh; brand mark 1rem"
      ],
      "measuredFrom": [
        "cite:nav-slide"
      ]
    }
  },
  "tokensUsed": [
    "semantic.color.text.muted",
    "semantic.color.text.primary",
    "semantic.color.surface.subtle",
    "semantic.color.border.default",
    "semantic.typography.bodyRegular"
  ],
  "accessibility": {
    "landmark": "banner + navigation",
    "keyboard": "Dropdown toggles are role=button with Enter/Space/Escape; hamburger is role=button with aria-expanded.",
    "measuredFrom": [
      "cite:nav-dropdown-timing"
    ]
  },
  "measuredFrom": [
    "cite:nav-menus",
    "cite:nav-dropdown-timing",
    "cite:nav-label-weight",
    "cite:nav-dropdown-panel",
    "cite:nav-buttons",
    "cite:nav-mobile-400",
    "cite:nav-hamburger",
    "cite:css-dropdown-transparent",
    "cite:css-nav-menu-link",
    "cite:nav-brand-rotate-src"
  ],
  "notes": [
    "Brand-mark hover spin (nav-logo-spin) is measured on live (live:nav-logo-spin) and in src (cite:nav-brand-rotate-src). It is not in the saved export because Webflow applies it at runtime. It is distinct from the logo strip, whose tiles never rotate."
  ]
}
